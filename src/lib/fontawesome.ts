import { config } from "@fortawesome/fontawesome-svg-core";

// Tell Font Awesome to skip adding its CSS automatically since it's
// imported once in the root layout — this prevents icons flashing
// large/unstyled on first paint.
config.autoAddCss = false;
